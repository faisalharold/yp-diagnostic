# yp-diagnostic
Diagnostic coordinate for finite systems near capacity thresholds. Implements y(p) = (1 − p₂/p₁)^(-1/2) to flag when rate-based metrics become unreliable and survival-based analysis is required.
